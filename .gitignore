# Artefactos de compilación (Build)
build/
build_disable_gen/
build_disable_gen2/

# Archivos de configuración generados localmente (mantén sdkconfig.defaults)
sdkconfig
sdkconfig.old

# Código generado por esp_board_manager (NO debe versionarse)
components/gen_bmgr_codes/

# Dependencias gestionadas por ESP-IDF (se descargan solas según idf_component.yml)
managed_components/
dependencies.lock
